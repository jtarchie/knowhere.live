version: 3

tasks:
  default:
    cmds:
      - deno fmt src/ functions/ docs/
      - deno lint src/ functions/
      - npx tsc
      - yarn run build
      - yarn run docs:build
      # - task: test
  server: yarn run local
  build: yarn run build --watch
  test: yarn run test